# Sincronizar AST com a Tabela de Simbolos
Pricipais problemas:
- Resolução de Dependencias

### Resolver Dependecias
ClassNode <- ClassInfo <- scopeAccessor
FuncNode <- FuncInfo <- scopeAccessor
*O simbolo (<-) significa 'depende de '.

# Geração de código
